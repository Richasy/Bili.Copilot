<?xml version="1.0" encoding="utf-8" ?>
<local:SettingsPageControlBase
    x:Class="BiliCopilot.UI.Controls.Settings.ScreenshotSettingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:Richasy.WinUIKernel.Share.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="using:BiliCopilot.UI.Extensions"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:local="using:BiliCopilot.UI.Controls.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <base:SettingsExpander
        x:Name="Exp"
        Description="{ext:Locale Name=ScreenshotSettingDescription}"
        Header="{ext:Locale Name=ScreenshotSetting}">
        <base:SettingsExpander.HeaderIcon>
            <ic:SymbolIcon Symbol="Screenshot" />
        </base:SettingsExpander.HeaderIcon>
        <base:SettingsExpander.Items>
            <base:SettingsCard
                x:Name="Folder"
                Description="{x:Bind ViewModel.ScreenshotFolderPath, Mode=OneWay}"
                Header="{ext:Locale Name=ScreenshotFolder}">
                <Button
                    MinWidth="120"
                    Command="{x:Bind ViewModel.PickScreenshotFolderCommand}"
                    Content="{ext:Locale Name=PickFolder}" />
            </base:SettingsCard>
            <base:SettingsCard
                x:Name="Behavior"
                Description="{ext:Locale Name=ScreenshotPostBehaviorDescription}"
                Header="{ext:Locale Name=ScreenshotPostBehavior}">
                <ComboBox
                    x:Name="BehaviorComboBox"
                    MinWidth="120"
                    SelectionChanged="OnBehaviorChanged">
                    <ComboBoxItem Content="{ext:Locale Name=Unset}" />
                    <ComboBoxItem Content="{ext:Locale Name=CopyImage}" />
                    <ComboBoxItem Content="{ext:Locale Name=OpenImage}" />
                </ComboBox>
            </base:SettingsCard>
        </base:SettingsExpander.Items>
    </base:SettingsExpander>
</local:SettingsPageControlBase>
